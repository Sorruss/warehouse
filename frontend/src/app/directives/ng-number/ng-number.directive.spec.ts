import { NgNumberDirective } from './ng-number.directive';

describe('NgNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NgNumberDirective();
    expect(directive).toBeTruthy();
  });
});
